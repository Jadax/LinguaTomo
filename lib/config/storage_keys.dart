/// Stable local storage names. The legacy value is migration-only and must not
/// appear in product copy, analytics or newly-created data.
abstract final class StorageKeys {
  static const userData = 'linguatomo_user_data';
  static const legacyUserData = 'nekokana_user_data';
}
